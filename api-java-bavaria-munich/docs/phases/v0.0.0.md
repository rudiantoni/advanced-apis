# v0.0.0 (Phase 0): Executable project scaffold

Goals:

1. A runnable project using Java 8 and Spring Boot 2.7, with a working build and smoke test.
2. A public endpoint for basic API validation (`GET /api/open/check`).

## Files

### `api-java-bavaria-munich/build.gradle`

Defines the Gradle project: Spring Boot plugins, Java 8 version, minimal dependencies (`web` + tests), and the test task.

```gradle
plugins {
    id 'java'
    id 'org.springframework.boot' version '2.7.18'
    id 'io.spring.dependency-management' version '1.0.15.RELEASE'
}

group = 'com.myapps'
version = '0.0.1-SNAPSHOT'

java {
    sourceCompatibility = JavaVersion.VERSION_1_8
    targetCompatibility = JavaVersion.VERSION_1_8
}

repositories {
    mavenCentral()
}

dependencies {
    implementation 'org.springframework.boot:spring-boot-starter-web'
    testImplementation 'org.springframework.boot:spring-boot-starter-test'
}

tasks.named('test') {
    useJUnitPlatform()
}
```

### `api-java-bavaria-munich/settings.gradle`

Gradle project name (used for the JAR name and module tasks).

```gradle
rootProject.name = 'api-java-bavaria-munich'
```

### `api-java-bavaria-munich/src/main/java/com/myapps/bavariamunich/ApiJavaBavariaMunichApplication.java`

Main class (`main`): starts Spring Boot and brings up the embedded server.

```java
package com.myapps.bavariamunich;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
public class ApiJavaBavariaMunichApplication {
    public static void main(String[] args) {
        SpringApplication.run(ApiJavaBavariaMunichApplication.class, args);
    }
}
```

### `api-java-bavaria-munich/src/test/java/com/myapps/bavariamunich/ApiJavaBavariaMunichApplicationTests.java`

Smoke test: verifies the Spring context starts without error (`contextLoads`).

```java
package com.myapps.bavariamunich;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

@SpringBootTest
class ApiJavaBavariaMunichApplicationTests {

	@Test
	void contextLoads() {
	}
}
```

### `api-java-bavaria-munich/src/main/resources/application.yml`

Spring configuration: port, API prefix (`/api`), and log level.

```yml
server:
  port: 8080
  servlet:
    context-path: /api

logging:
  level:
    root: INFO
```

### `api-java-bavaria-munich/src/main/java/com/myapps/bavariamunich/controller/OpenController.java`

Phase 0 public controller to verify the API is reachable without authentication.

```java
package com.myapps.bavariamunich.controller;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
import org.springframework.http.HttpStatus;
import org.springframework.http.ResponseEntity;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;

import java.util.Collections;
import java.util.Map;

@RestController
@RequestMapping("/open")
public class OpenController {

    private static final Logger logger = LoggerFactory.getLogger(OpenController.class);

    @GetMapping("/check")
    public ResponseEntity<Map<String, String>> readAll() {
        String msg = "OPEN CHECK: You reached the open endpoint SUCCESSFULLY.";
        logger.info(msg);
        Map<String, String> result = Collections.singletonMap("message", msg);
        return new ResponseEntity<>(result, HttpStatus.OK);
    }
}
```

When calling `GET /api/open/check`, expect status `200 OK` with the following response body:

```json
{
  "message": "OPEN CHECK: You reached the open endpoint SUCCESSFULLY."
}
```
